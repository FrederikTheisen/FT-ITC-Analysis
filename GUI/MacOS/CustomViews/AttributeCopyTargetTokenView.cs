// This file has been autogenerated from a class added in the UI designer.

using System;
using Foundation;
using AppKit;

namespace AnalysisITC
{
    public partial class AttributeCopyTargetTokenView : NSView
    {
        public AttributeCopyTargetTokenView(IntPtr handle) : base(handle)
        {
        }

        partial void Action(NSTextField sender)
        {
            string token = sender.StringValue.Trim().ToLower();

            // Ignore if token empty
            if (string.IsNullOrWhiteSpace(token)) return;

            AppEventHandler.Print($"Copying selected attributes to experiments matching name token: {token}");

            DataManager.CopySelectedAttributesToNameToken(token);
            DataManager.InvokeUpdateTable();
        }
    }
}
